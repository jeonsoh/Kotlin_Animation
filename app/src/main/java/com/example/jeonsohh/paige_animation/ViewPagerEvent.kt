package com.example.jeonsohh.paige_animation

import android.support.v4.view.ViewPager

class ViewPagerEvent(d : Int) { //0 : left    1 : right
    var direction : Int
    init{
        direction = d
    }
}